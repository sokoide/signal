#define _POSIX_C_SOURCE 200809L
#include <signal.h>
#include <stdio.h>
#include <unistd.h>
int main(void) {
    /* TODO(step 6): synchronously receive a blocked signal with sigwaitinfo. */
    puts("TODO step 6: block the set and receive it with sigwaitinfo");
    return 2;
}
